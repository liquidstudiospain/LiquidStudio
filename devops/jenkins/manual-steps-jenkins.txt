manual steps jenkins

wget -q -O - https://pkg.jenkins.io/debian/jenkins-ci.org.key | sudo apt-key add -
sudo sh -c 'echo deb http://pkg.jenkins.io/debian-stable binary/ > /etc/apt/sources.list.d/jenkins.list'
sudo apt-get update
sudo apt-get install jenkins

configurar nginx - https://medium.com/google-cloud/node-to-google-cloud-compute-engine-in-25-minutes-7188830d884e

sudo apt-get install -y nginx
cd /etc/nginx/sites-available
sudo mv default default.bak
sudo touch default
//// START
server {
  listen 80;
  server_name YOUR_SERVERS_IP_ADDRESS;

  location / {
    proxy_pass "http://127.0.0.1:8080";
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_cache_bypass $http_upgrade;
  }
}
///// END
sudo service nginx restart


//// YA DEBERIA ESTAR JENKINS DISPONIBLE EN EL PUERTO 80

Añadir credenciales:
	Ir a JENKINS_SERVER/credentials -> jenkins -> global -> add credentials y añadir desde ahi (desde el otro lado siempre falla)
		1) api token
		2) username y password